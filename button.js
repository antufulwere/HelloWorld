import React from "react";
import "./style.css";
const Button = ({ btntext }) => {
  //this are a arow function
  return <button className="btn">{btntext}</button>;
};

export default Button;
